import AbstractAlgorithmFactory from "./AbstractAlgorithmFactory"

export default class DC140713AlgorithmFactory extends AbstractAlgorithmFactory {
    constructor(algorithm) {
        super(algorithm);
    }
}
